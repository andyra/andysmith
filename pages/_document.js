import { Html, Head, Main, NextScript } from "next/document";

const Document = () => (
  <Html className="scroll-smooth" lang="en">
    <Head />
    <body className="bg-ground font-base text-base text-primary">
      <Main />
      <NextScript />
    </body>
  </Html>
);

export default Document;
